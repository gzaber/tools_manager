enum ToolsViewOption { inToolRoom, atUsers, pending }
enum ToolStatusByUserOption { added, given, received, transferred }

enum ManageToolSuccessInfo { toolAdded, toolUpdated, toolDeleted }
enum ManageUserSuccessInfo { userAdded, userUpdated, userDeleted }
